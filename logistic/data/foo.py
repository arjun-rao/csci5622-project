# coding: utf-8
ip_file = "bio_probs.txt
ip_file = "bio_probs.txt"
pos_tags = []
with open(ip_file, 'r') as fp:
    lines = [line.strip() for line in fp]
    print(lines[4])
    
with open(ip_file, 'r') as fp:
    lines = [line.strip() for line in fp]
    print(lines)
    
    
with open(ip_file, 'r') as fp:
    lines = [line.strip() for line in fp]
    print(lines[0])
    
    
    
with open(ip_file, 'r') as fp:
    lines = [line.strip() for line in fp]
    print(len(lines))
    
    
    
    
with open(ip_file, 'r') as fp:
    lines = [line for line in fp]
    print(lines)
    
    
    
    
    
with open(ip_file, 'r') as fp:
    lines = [line.split() for line in fp]
    print(lines) 
    
liens
lines
len(lines)
len(lines[0])
len(lines[1])
a = []
if a:
    print("h")
    
pos_tags
for i in lines:
    if i:
        pos_tags.append(i[4])
        
len(pos_tags)
len(lineS)
len(lines)
list(set(pos_tags))
pos_tags = list(set(pos_tags))
pos_tags
len(pos_tags)
get_ipython().run_line_magic('save', 'foo.py 1-26')
